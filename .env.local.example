# Base URL of the Spring Boot backend API.
# Local dev default is http://localhost:8081 (see lib/api.ts).
# In Vercel, set this to your deployed Render backend URL, e.g.
# https://greenlife-spaces-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8081
